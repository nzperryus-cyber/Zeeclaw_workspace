# Daily Memory — 2026-03-27

## Memory System Updates

### LCM (Lossless-Claw) Installed
- Plugin installed for permanent conversation history
- Uses DAG-based summarization — never loses context
- Configured to use ollama/phi4-mini for summarization (local, free)
- No longer burning MiniMax credits on background summarization
- Database: ~/.openclaw/lcm.db

### phi4-mini Model Pulled
- 3.8B model, ~2.5GB
- Handles LCM summarization entirely offline
- Ollama now has: phi4-mini, nomic-embed-text, gemma3:4b, qwen2.5vl:7b, gemma3:12b, qwen3:30b-a3b

### Cron Jobs Fixed
- Missing UUIDs added to 4 cron jobs (End-of-Day, Morning Briefing, Weekly Evolver, Weekly Maintenance)
- Duplicates removed
- Morning briefing now runs script directly (no LLM reasoning) — 30s timeout
- End-of-day now runs script directly — 30s timeout (previously timed out at 120s due to LLM cold start)
- Weekly OpenClaw Self-Update corrected from 7am weekdays → Sunday 6am

### Telegram Token Fix
- Bot token was in openclaw.json but morning-briefing.sh was looking in ~/.zeeclaw/credentials.json
- Added TELEGRAM_BOT_TOKEN to credentials.json — fixed morning briefing send

## Projects Updated
- 25-71662: due date corrected to 2026-04-07 (was 03/23 in some places)
- 25-67725: fixed index typo "rev rev 1" → "sent rev 1"
- 25-72313: status confirmed as "sent" not "awarded"

## Daily Ontology Sync Cron Created
- Runs weekdays at 6am
- Reads previous day's session transcripts
- Extracts project updates, status changes, new facts
- Updates ontology graph automatically
- Addresses the habit problem (wasn't using ontology consistently)

## Memory Architecture (Final)
Layered system:
1. LCM — permanent conversation history (LCM plugin)
2. Ontology + daily sync — structured facts, automated updates
3. MEMORY.md + daily logs — curated long-term memory
4. Scripts (morning-briefing, end-of-day, etc.) — automated check-ins

## Notes
- Nathan's MiniMax plan costs $10/month for 100 calls per 5 hours
- LCM summarization now free (local phi4-mini)
- nomic-embed-text still installed but unused (was for cognee, which we removed)
